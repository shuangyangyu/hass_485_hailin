# 海林环境监测仪 Modbus 数据帧解析

## 概述

本文档详细描述了海林环境监测仪的Modbus数据帧格式和解析方法，包含PM2.5、温度、湿度三种环境参数的数据读取。

## 数据帧结构

### 基本信息表

| 字段 | 值 | 十六进制 | 十进制 | 实际值 | 说明 |
|------|----|---------|---------|---------|---------| 
| 从机地址 | 01 | 0x01 | 1 | 设备地址1 | Modbus设备地址 |
| 功能码 | 03 | 0x03 | 3 | 读保持寄存器 | Modbus功能码 |
| 数据长度 | 06 | 0x06 | 6 | 6字节数据 | 后续数据字节数 |

### 传感器数据表

| 传感器类型 | 寄存器地址 | 原始值(HEX) | 原始值(DEC) | 换算公式 | 最终值 | 单位 |
|------------|------------|-------------|-------------|----------|--------|------|
| PM2.5 | 寄存器0 | 00 12 | 18 | 直接显示 | 18 | μg/m³ |
| 温度 | 寄存器1 | 01 1D | 285 | 值 ÷ 10 | 28.5 | °C |
| 湿度 | 寄存器2 | 00 35 | 53 | 直接显示 | 53 | % RH |

## 数据帧校验

### CRC校验信息

| 校验项目 | 值 | 说明 |
|----------|-----|------|
| CRC校验 | C8 9B | 因PM2.5数值变化，CRC重新计算 |
| 校验状态 | ✅ 通过 | 符合Modbus CRC校验规则 |

## 完整数据帧示例

### 请求帧
```
01 03 00 00 00 03 05 CB
```

**解析：**
- `01`：从机地址
- `03`：功能码（读保持寄存器）
- `00 00`：起始寄存器地址（0）
- `00 03`：读取寄存器数量（3个）
- `05 CB`：CRC校验

### 响应帧
```
01 03 06 00 12 01 1D 00 35 C8 9B
```

**解析：**
- `01`：从机地址
- `03`：功能码
- `06`：数据长度（6字节）
- `00 12`：PM2.5值（18 μg/m³）
- `01 1D`：温度值（285 → 28.5°C）
- `00 35`：湿度值（53% RH）
- `C8 9B`：CRC校验

## 数据解析算法

### Python代码示例

```python
def parse_hailin_data(response):
    """
    解析海林环境监测仪数据
    
    Args:
        response: Modbus响应数据（字节数组）
        
    Returns:
        dict: 包含PM2.5、温度、湿度的字典
    """
    if len(response) < 9:
        raise ValueError("数据长度不足")
    
    # 提取传感器数据（跳过地址、功能码、长度字段）
    data = response[3:9]
    
    # PM2.5（寄存器0）- 直接显示
    pm25_raw = (data[0] << 8) | data[1]
    pm25 = pm25_raw  # 直接显示，单位 μg/m³
    
    # 温度（寄存器1）- 需要除以10
    temp_raw = (data[2] << 8) | data[3]
    temperature = temp_raw / 10.0  # 单位 °C
    
    # 湿度（寄存器2）- 直接显示
    humid_raw = (data[4] << 8) | data[5]
    humidity = humid_raw  # 直接显示，单位 % RH
    
    return {
        'pm25': pm25,
        'temperature': temperature,
        'humidity': humidity
    }

# 示例使用
response_data = bytes.fromhex("01 03 06 00 12 01 1D 00 35 C8 9B")
result = parse_hailin_data(response_data)
print(f"PM2.5: {result['pm25']} μg/m³")
print(f"温度: {result['temperature']} °C")
print(f"湿度: {result['humidity']} % RH")
```

## 寄存器映射

| 寄存器地址 | 数据类型 | 描述 | 数据格式 | 取值范围 |
|------------|----------|------|----------|----------|
| 0x0000 | UINT16 | PM2.5浓度 | 直接值 | 0-999 μg/m³ |
| 0x0001 | UINT16 | 温度 | 实际值×10 | -400-1000 (对应-40.0-100.0°C) |
| 0x0002 | UINT16 | 湿度 | 直接值 | 0-100 % RH |

## 通信参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 波特率 | 9600 | 默认通信速率 |
| 数据位 | 8 | 数据位数 |
| 停止位 | 1 | 停止位数 |
| 校验位 | None | 无校验 |
| 从机地址 | 1-247 | 可配置范围 |

## 错误处理

### 常见错误代码

| 错误代码 | 描述 | 解决方案 |
|----------|------|----------|
| 0x01 | 非法功能码 | 检查功能码是否为0x03 |
| 0x02 | 非法数据地址 | 确认寄存器地址正确 |
| 0x03 | 非法数据值 | 检查请求数据数量 |
| 0x04 | 从机设备故障 | 重启设备或检查硬件 |

### CRC校验失败处理

```python
def verify_crc(data):
    """验证Modbus CRC校验"""
    if len(data) < 3:
        return False
    
    # 提取数据和CRC
    message = data[:-2]
    received_crc = (data[-1] << 8) | data[-2]
    
    # 计算CRC
    calculated_crc = calculate_modbus_crc(message)
    
    return received_crc == calculated_crc
```

## 集成说明

此数据帧格式已成功集成到Home Assistant的海林Modbus自定义组件中，提供实时环境监测数据。

### 相关文件
- `sensor.py`：传感器实体定义
- `__init__.py`：组件初始化
- `config_flow.py`：配置流程

### 版本信息
- 当前版本：v0.1.8
- 支持的设备：海林环境监测仪
- 协议版本：Modbus RTU/TCP

---

*本文档基于海林环境监测仪实际测试数据编写，确保数据解析的准确性和可靠性。* 